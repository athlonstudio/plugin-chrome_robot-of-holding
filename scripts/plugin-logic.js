const page = '';
const setPage = (pageKey) => {

}
const pageKeys = {
    LANDING: 'landing',
    ONBOARDING: 'onboarding',
    SETTINGS: 'settings',
};

function loadIcons() {
}
console.log('hi')
document.querySelector('#list_name').innerHTML = window.location.href;
//set the page to default and render it
//render all the icons
//get the list name and id from the history
//